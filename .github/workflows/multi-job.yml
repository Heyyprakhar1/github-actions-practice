name: multi-job
on:
    workflow_dispatch:

jobs:
    multi-jobs:
        runs-on: ubuntu-latest
        steps:
            - run: echo "Building the app..!"

    test:
        runs-on: ubuntu-latest
        needs: [multi-jobs]
        steps:
            - run: echo "Running test..!"

    build:
        runs-on: ubuntu-latest
        needs: [test]
        steps:
            - run: echo "Deployement done..!"
